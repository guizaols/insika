# harvest/CRITERION.md — frozen BEFORE the first promotion (RFC §4.3).

> A skill may land only when the store's ruler is not measurably worse than
> the accepted state; outcome is evidence in the gate, never the decider
> alone. The store's real numbers are the forge's — re-freeze this file
> before the pilot's first promotion; the engine compares against this sha.

```yaml
version: 1
metric: primary
window: 72h
threshold: 0.05
min_span: 28d
```